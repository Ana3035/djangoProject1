"""
第一范式：列不可拆分，
第二范式：唯一标识，比如说名字不能重复
第三范式：引用主键，一个文件可以由多条数据，一行相当于一条数据，一个文件里面的主键是唯一的，比如说第一个

ER模型：当前物理的数据库都是按章ER模型来设立的，E表示的是实体R表示的是关系，一个实体能够转换为数据库中的一个表，比如班级是一个实体，是一个表表里面存储的数据就是每一个人的信息，关系描述了两个实体之间的对应规则，比如每个班级表

数据库的操作：创建和删除
表的操作：创建修改和删除
数据的操作：增删改除简称CRUD

一个数据库是一个完整的单元，而数据库存储数据是存在表当中，每一类数据放在一张表当中，bit表示的是bool类型的数据
存储数据首先需要设计表，比如说创建学生的数据，学号可以作为主键，因为每一个学生的学号是不一样的，之后比如姓名，性别，年龄，这些都是表的结构，之后再向表中存储数据

一般删除数据的时候，都是逻辑删除，可以添加一个逻辑字符串delete，采用的是bit类型，0表示的是没有删除，1表示的是删除，但是实际上是没有删除的,等到excel筛选的时候，直接过滤，表示的就是逻辑删除
"""